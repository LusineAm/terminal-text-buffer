# terminal buffer

initial Kotlin project setup for terminal text buffer

this repository currently contains only the project scaffold:

- Gradle with Kotlin DSL
- Kotlin JVM configuration
- JUnit 5 test setup

build the project:

```
./gradlew build
```

run the current verification task:

```
./gradlew test
```
